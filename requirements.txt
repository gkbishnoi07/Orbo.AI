# Runtime dependencies for the deployed app.
#
# Deliberately does NOT include sentence-transformers. Embeddings are computed
# offline by scripts/02_embed.py and committed as .npy, so the deployed app
# never needs torch. That keeps the image inside a free tier's disk limit and
# inference well under 100ms. Heavy tooling lives in requirements-dev.txt.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
streamlit>=1.30
pyarrow>=14.0  # reads the committed .parquet artifacts
